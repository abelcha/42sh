t_token			*get_tokens(char *str)
{
  t_token		*root;

  root = init_root();
  if (!root)
    return (NULL);
  root->data = str;
  if (tokenizer_parsing(root, 0) == FAILURE)
    return (NULL);
  return (root);
}
