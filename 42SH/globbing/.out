int		get_command(char *word, t_line *line, t_gb *gb)
{
  char		**p_tab;
  char		*tmp;
  int		i;

  i = -1;
  p_tab = line->exe->env->paths;
  while (p_tab && p_tab[++i])
    get_data(p_tab[i], word, gb);
  return (SUCCESS);
}
